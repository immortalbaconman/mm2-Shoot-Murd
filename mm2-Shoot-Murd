local MURD = nil 
local players = game:GetService("Players")
local UIS = game:GetService("UserInputService")


UIS.InputBegan:Connect(function(input, busy)
  if input.KeyCode == Enum.KeyCode.C  and not busy then
 for _, v in pairs(game:GetService("Players"):GetChildren()) do
        if v.Backpack:FindFirstChild("Knife") then
            MURD = v.Name
        elseif not v.Backpack:FindFirstChild("Knife") then
            if v.Character:FindFirstChild("Knife") then
                MURD = v.Name
        end
    end
end

game:GetService("Players").LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(Players[MURD].Character.HumanoidRootPart.CFrame.X, Players[MURD].Character.HumanoidRootPart.CFrame.Y, Players[MURD].Character.HumanoidRootPart.CFrame.Z + 4))

wait(.3)

local args = {
    [1] = 1,
    [2] = Vector3.new(Players[MURD].Character.HumanoidRootPart.CFrame.X,Players[MURD].Character.HumanoidRootPart.CFrame.Y,Players[MURD].Character.HumanoidRootPart.CFrame.Z),
    [3] = "AH"
}

game:GetService("Players").LocalPlayer.Character.Gun.KnifeServer.ShootGun:InvokeServer(unpack(args))
--END
   end
end)
